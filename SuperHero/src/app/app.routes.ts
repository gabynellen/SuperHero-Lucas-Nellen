import { Routes } from '@angular/router';
import { SuperHeroComponent } from './presentation/super-hero/super-hero.component';

export const routes: Routes = [{ path: '', component: SuperHeroComponent }];
